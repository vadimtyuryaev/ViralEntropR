# ViralEntropR

**ViralEntropR** provides a statistical framework for detecting emerging viral variants from time-series sequence data. It combines Shannon entropy clustering with Hellinger distance metrics and non-parametric change point analysis.

## Installation

You can install the development version of ViralEntropR like so:

```r
# install.packages("devtools")
devtools::install("path/to/ViralEntropR")